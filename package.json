{
  "name": "neutralpress-cloud",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check-types": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 execute NP_CLOUD_DB --local --file=./schema/init.sql",
    "db:migrate:remote": "wrangler d1 execute NP_CLOUD_DB --remote --file=./schema/init.sql"
  },
  "dependencies": {
    "hono": "^4.8.5",
    "jose": "^5.9.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260214.0",
    "typescript": "^5.7.3",
    "wrangler": "^4.25.0"
  }
}
